// proyectovisor4

// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyBZ4l1moqCWT6w_1I47Q7jov6ZcJciW-7E",
  authDomain: "visor-proyecto-sgc01.firebaseapp.com",
  projectId: "visor-proyecto-sgc01",
  storageBucket: "visor-proyecto-sgc01.appspot.com",
  messagingSenderId: "439721075291",
  appId: "1:439721075291:web:4158b30ea9bc81740d557e"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);


// visor.psgc


        const firebaseConfig = {
            apiKey: "AIzaSyDfiyD8OisRg9OvfmmulQW-FRfSz9Bi_PU",
            authDomain: "visor-proyecto-sgc.firebaseapp.com",
            projectId: "visor-proyecto-sgc",
            storageBucket: "visor-proyecto-sgc.appspot.com",
            messagingSenderId: "629279692498",
            appId: "1:629279692498:web:eb4a6c6ab26835274abf0b",
            measurementId: "G-85NGWBCYEV"
        };

        // Initialize Firebase
        const app = firebase.initializeApp(firebaseConfig);
        const database = firebase.database();
        var storage = firebase.storage();
        var storageRef = storage.ref();







      //   function justType(type, TYPE) {
      //     return function(gj) {
      //         var oftype = gj.features.filter(isType(type));
      //         return {
      //             geometries: oftype.map(justCoords),
      //             properties: oftype.map(justProps),
      //             type: TYPE
      //         };
      //     };
      // }
      
      // function justCoords(t) {
      //   return t.geometry.coordinates;
      // }
      
      // function justProps(t) {
      //     return t.properties;
      // }
      
      // function isType(t) {
      //   return function(f) { return f.geometry.type.replace('Multi', '') === t; };
      // }




      